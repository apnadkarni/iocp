
    load ../win/Release_AMD64_VC1916/iocp01t.dll


	fileevent stdin readable exit
	set server [iocp::socket -server accept -myaddr 127.0.0.1 0]
	proc accept {s h p} {puts $s ok; close $s; set ::x 1}
	puts [lindex [fconfigure $server -sockname] 2]
	flush stdout
	vwait x
    
